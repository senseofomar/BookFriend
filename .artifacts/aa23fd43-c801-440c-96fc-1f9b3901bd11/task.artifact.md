# Tasks: Streamlit Community Cloud Deployment

- `[ ]` Create unified `app.py` that calls logic functions directly (avoiding HTTP/FastAPI dependencies)
- `[ ]` Update `requirements.txt` to include all necessary dependencies for a single-process run
- `[ ]` Verify `app.py` works locally by running `streamlit run app.py`
- `[ ]` Provide "Secrets" TOML configuration for the Streamlit dashboard
- `[ ]` Finalize walkthrough with deployment instructions
